uos.types['node_device'] = {};

uos.types['node_device'].extends = null;

uos.types['node_device'].actions = {

	deviceon : {
		title : 'On',
		icon : 'fa-power-off',	
	},
	
	deviceoff : {
		title : 'Off',
		icon : 'fa-power-off',				
	},
};


// functions for now should be named :
/// function uostype_ENTITYTYPE_hander($element,data)